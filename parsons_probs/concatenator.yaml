problem_name: concatenator

problem_description: |
  In this exercise you will practice working with Python string values.

task_instructions: |
  <code>concatenator</code>
    returns a string with all the contents of the list concatenated together.

  <pre><code>
  >>> concatenator(["Super", "cali", "fragilistic", "expialidocious"])
  'Supercalifragilisticexpialidocious'
  >>> concatenator(["finger", "spitzen", "gefühl"])
  'fingerspitzengefühl'
  >>> concatenator(["none", "the", "less"])
  'nonetheless'
  </code></pre>

code_lines: |
  def concatenator(items): #0given
  combined = !BLANK
  for !BLANK in !BLANK:
  combined += !BLANK
  return combined

test_fn: concatenator