problem_name: greater_num

problem_description: |
  In this exercise you will practice working with Python comparison operators.

task_instructions: |
  <code>greater_num</code>
    returns whichever number is highest of the two supplied numbers.

  <pre><code>
  >>> greater_num(45, 10)
  45
  >>> greater_num(-1, 30)
  30
  >>> greater_num(20, 20)
  20
  </code></pre>

code_lines: |
  def greater_num(num1, num2): #0given
  if !BLANK:
  return !BLANK
  else:
  return !BLANK

test_fn: greater_num